name: Build & Deploy Histoire

on:
  push:
    branches: ["main"]
    paths:
      - "packages/vue/**"
      - "packages/styles/**"
      - "packages/icons/**"

  pull_request:
    branches: ["main"]
    paths:
      - "packages/vue/**"
      - "packages/styles/**"
      - "packages/icons/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          run_install: false
      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies
        run: pnpm install
      - name: Build
        run: pnpm build
      - name: Package
        uses: a7ul/tar-action@v1.1.0
        with:
          command: c
          cwd: "./packages/vue"
          files: |
            Dockerfile
            .histoire/*
            captain-definition
          outPath: deploy.tar
      - name: Deploy to CapRover
        uses: caprover/deploy-from-github@v1.0.1
        with:
          server: "${{ secrets.CAPROVER_HISTOIRE_SERVER }}"
          app: "${{ secrets.CAPROVER_HISTOIRE_APP_NAME }}"
          token: "${{ secrets.CAPROVER_HISTOIRE_APP_TOKEN }}"
