---
import BaseLayout from './Layout.astro';
import { components } from '../navigation';

interface Props {
  title: string
}

const { title } = Astro.props as Props;
---

<BaseLayout title={title} sidebar={components} withSidebar>
  <slot />
</BaseLayout>