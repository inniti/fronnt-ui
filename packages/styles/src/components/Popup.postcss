/*! @fronntui/styles NNPopup */

.nn-popup {
  border-radius: var(--nn-popup-border-radius);
  padding: var(--nn-spacing-1);
  box-shadow: var(--nn-shadow-lg);
  background: white;
  box-sizing: border-box;
  width: min-content;
}
